server {
    listen 80;
    server_name primuslugi.ruslandev.uz;

    location / {
        proxy_pass http://localhost:3005;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Security headers
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Optional: Increase client body size for media uploads
    client_max_body_size 20M;
}

# SSL Configuration (using Certbot)
# server {
#     listen 443 ssl;
#     server_name primuslugi.ruslandev.uz;
#
#     ssl_certificate /etc/letsencrypt/live/primuslugi.ruslandev.uz/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/primuslugi.ruslandev.uz/privkey.pem;
#
#     location / {
#         proxy_pass http://localhost:3005;
#         ...
#     }
# }
